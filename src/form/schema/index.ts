export type addCommentsFormTypes = {
  name: string;
  email: string;
  body: string;
};

export type addUsersFormTypes = {
  name: string;
  username: string;
  email: string;
  street: string;
  suite: string;
  city: string;
  zipCode: string;
  latitude: string;
  longitude: string;
  phone: string;
  website: string;
  companyName: string;
  catchPhrase: string;
  Bs: string;
};
